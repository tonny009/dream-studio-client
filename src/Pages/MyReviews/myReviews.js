import React from 'react';
import useTitle from '../../hooks/useTitle';

const MyReviews = () => {
    useTitle('DS/My Reviews')
    return (
        <div>
            <h1>Here are my reviews</h1>
        </div>
    );
};

export default MyReviews;